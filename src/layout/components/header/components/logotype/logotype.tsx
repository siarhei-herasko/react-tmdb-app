import React from 'react';
import {Typography} from '@mui/material';

const Logotype = () => {
    return (
        <Typography variant="overline" fontSize="1.5rem" fontWeight="bold">
            TMDB
        </Typography>
    );
};

export default Logotype;
